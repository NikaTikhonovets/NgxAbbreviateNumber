import { NgxAbbreviateNumberPipe } from './ngx-abbreviate-number.pipe';

describe('NgxAbbreviateNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new NgxAbbreviateNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
